# Hook System

This package provides a simple hook/event system for plugins and the core to communicate via public events. Plugins can register and trigger hooks without direct references to each other.
